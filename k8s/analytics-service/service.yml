apiVersion: v1
kind: Service

metadata:
  name: analytics-service
  namespace: tiny-url

spec:
  selector:
    app: analytics-service

  ports:
    - port: 8080
      targetPort: 8080

  type: ClusterIP