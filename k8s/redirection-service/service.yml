apiVersion: v1
kind: Service

metadata:
  name: redirection-service
  namespace: tiny-url

spec:
  selector:
    app: redirection-service

  ports:
    - port: 8082
      targetPort: 8082

  type: ClusterIP